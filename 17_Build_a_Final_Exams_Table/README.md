# Calculus Grades Table - HTML Table Assignment

A basic HTML table displaying calculus exam grades with semantic table structure, including header, body, footer, and caption elements.

## 📖 About This Project

This assignment demonstrates HTML table implementation with proper semantic structure. The project continues the pattern of building unstyled, static HTML pages without CSS or functionality.

🧠 What I Learned
1. HTML table structure implementing `<table>`, `<thead>`, `<tbody>`, `<tfoot>`.
2. Table semantics proper use of table caption and header cells.
3. Data organization structuring tabular data with row and column headers.
4. Column spanning using `colspan` for footer cells.
5. Tabular data presentation organizing grade information in table format.
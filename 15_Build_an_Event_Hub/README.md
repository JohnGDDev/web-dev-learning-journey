# Event Hub - HTML Structure Assignment

An events listing page with semantic HTML structure, featuring upcoming and past events with descriptive content and images.

## 📖 About This Project

This assignment continues the pattern of building unstyled HTML pages, focusing on event content organization with semantic structure. The project demonstrates content segmentation for event listings while maintaining HTML-only implementation.

🧠 What I Learned
- Event content structure organizing events into logical sections.
- Semantic segmentation using `<section>` and `<article>` for content organization.
- Navigation implementation internal page navigation between sections.
- Content hierarchy proper heading structure for event information.
- Media integration image elements with descriptive alt text.